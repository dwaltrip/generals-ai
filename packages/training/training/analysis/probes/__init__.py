"""Frozen-representation probe framework.

The task-agnostic machinery (`core.py`) plus the cross-player-softmax kit
(`cross_player.py`) that the "pick a player" probes share. Concrete probes are
one-off scripts in the sibling `probe_runs/` package.
"""
