
# Manifests at `data/training/splits/` stay tracked
training/bench
training/probes
# Per-run training artifacts (logs, provenance, checkpoints, etc).
training/runs
training/runs-cloud
training/sweeps
# Eval: map packs are regenerable (tracked manifest + DB); runs-cloud holds
# pulled cloud-run artifacts.
eval/map-sets/*/maps-*.jsonl.gz
eval/runs-cloud
