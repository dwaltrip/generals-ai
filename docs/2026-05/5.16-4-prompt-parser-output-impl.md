# Handoff — Implement Parser Output v1

**Date:** 2026-05-16
**Status:** Design landed (5.16-3) after a fresh-perspective review pass. Implementation queued for this session.

## Read first

- **[`docs/2026-05/5.16-3-parser-output-design.md`](./5.16-3-parser-output-design.md)** — primary doc. Pinned schemas, sim_core additions, implementation seams, decision rationale. §7 is the scope for this session.

## Companion docs

- `docs/replay-parser-status.md` — parser sub-project state
- `docs/replay-parser-design.md` §3, §5, §10 — design context the 5.16-3 doc builds on
- `docs/2026-05/5.13-1-parser-implementation-plan.md` §9 — action-stream recording semantics (referenced from 5.16-3 §2.5)
- `sim-core/README.md` — timestep / snapshot / event convention table (load-bearing for the `cities_present_at` `+1` semantics)

## Scope

Per 5.16-3 §7:

1. **Rust** — add `actions_source/dest/is50` + `cities_present_at` to `sim_core::State`. Seams in §3.1, §3.2.
2. **Python** — `replay-parser/replay_parser/output.py` (sim-output writer + meta sidecar writer) + stubbed `replay-parser/replay_parser/metadata.py`. Signatures in §6.2, §6.3.
3. **Smoke test** — 2–3 fixtures from `replay-parser/tests/fixtures/` covering the scenarios listed in §7.

If time: real rolling-rate DB walk in `metadata.py`. Otherwise leave stubbed (`-1.0` / `-1` sentinels).

### Plan context: Bigger pricture arc

This work will unlock remaining major mising tasks needed for actual training runs:

- Initial v1 implementation of the actual neural net model
- Lock in final decisions for the v1 observation tensor
- Minimal training pipeline and related infra
- Any lingering training methodology design choices - e.g. hyperparams, etc.
- Test locally with CPU and hopefully apple silicon GPU
- Minimal test run with GPU provider (probably modal.com or something similar)

## Code entry points

- `sim-core/src/state.rs` — `State` struct, `step()`, `select_candidates()`, `execute_player_capture()`, `try_neutralize_player()`. All Rust hook points live here.
- `sim-core/src/lib.rs` — `simulate()` entry; getter exposure.
- `sim-core/python/sim_core/_native.pyi` — update type stubs when adding getters.
- `replay-parser/replay_parser/parser.py` — top-level `parse_replay`.
- `replay-parser/tests/` — fixture infrastructure (`_fixture_lib.py`, `test_sim_integration.py`).

## Heads-up for the implementer

- **Read-Clean Check applies to the new code.** No deprecation shims, no-op compat wrappers, or "restored" comments. The `actions_*` fields were previously dropped (memory entry `project_actions_perspective_deferred`); re-adding should land as native current-design code, not as a restoration.
- **Fixtures: `wire.bin` files are reusable as smoke-test inputs; no oracles for the new writer schema yet.** A proper fixture-regression test for `write_sim_output` (analogous to `test_sim_integration.py`) is deferred — needs the writer's output hand-validated on a few replays first, same pattern as the sim fixtures.

---

Please get oriented (5.16-3 is the load-bearing read; the companions are skim-as-needed), then check in before diving into code.

Let's make sure we are aligned before proceeding with any code.
