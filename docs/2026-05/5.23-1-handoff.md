## Handoff — cloud-real-mvp post-AMP

Current state:

* T4 cloud entrypoint is live.
* AMP landed (+36% throughput, 31% → 42% MFU). 
* We have MFU + GPU-util + log-timestamp instrumentation.
* Tracker is up to date.

The open question is whether we're ready to commit to the first real run (11–22 days at current rates is sobering).

**This session:**

1. Larger-slice "realistic" diagnostic run on T4 to verify throughput holds at scale beyond the 50-batch probes. Let's keep it short while still being informative. My current thought is: more data (bigger slice), bump up the max-batch number, possibly try a few batch-size variants. But I'd like to hear your thoughts as well.
2. Batch-size sweep under FP16 — activation memory is halved, so bs=512/1024 may unlock more.
3. Double-check we're getting the most out of the hardware with reasonable effort before committing.
4. Then revisit the real-run sizing (epoch count, corpus size, H100 question).

**Read first:**
- [`docs/2026-05/5.22-2-cloud-real-mvp.md`](./docs/2026-05/5.22-2-cloud-real-mvp.md) — tracker; see "Next session — gates before the first real run" + the runtime math table
- [`docs/gpu-performance-mental-model.md`](./docs/gpu-performance-mental-model.md) — evergreen framing (bottlenecks, roofline, tensor cores, precision formats)
- `git log --oneline --decorate=short -8` — recent arc

---

Check in once oriented and we can discuss how to move forward.
