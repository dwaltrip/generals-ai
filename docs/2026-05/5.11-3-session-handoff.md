# Handoff — generals-ai replay parser

Continuing work on the replay-parser/simulator sub-project. Phase 1 (JS bundle reading + canonical doc updates) just completed on 2026-05-11; Phase 2 (stand up `replay-parser/` subproject) is up next.

## Read in this order to get oriented (skim, don't deep-read)

1. `docs/replay-parser-status.md` — current state + phase plan. Entry point.
2. `docs/replay-parser-design.md` — design spec. §3 (output format), §4 (filters), §7 (implementation surface + simulator orchestration order), §10 (validation), §11 (resolved mechanics).
3. `docs/generals-io-game-mechanics.md` — game rules. §6, §7, §9 are load-bearing for the simulator.
4. `docs/game-mechanics-appendix-resolved-ambiguities.md` — companion bundle line refs for the resolved ambiguities.
5. `docs/replay-format.md` — `.gior` wire format reference.

## Reference material (don't read end-to-end; navigate as needed)

- `docs/2026-05/5.11-2-bundle-reading.md` — trace of Phase 1's findings + bundle line refs.
- `research/gior-format/generals-main-prod-v31.4.1-d51b92c0.js` — live JS bundle (~136k lines). Replay-mode game class at line ~67495.

## Once oriented

Likely tasks to work on next:

* Phase 2 = stand up `replay-parser/` as a sibling subproject to `replay-collector/`.
* NumPy parser; reads gzipped `wire_data` BLOBs via `replay_collector.wire.decode`.

Check in with me once you are feeling oriented and we can discuss next steps and implementation details.
