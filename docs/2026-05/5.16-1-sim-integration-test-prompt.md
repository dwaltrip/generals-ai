
# Handoff — fixture regression tests

**Last session landed:** per-snapshot invariant sweep (commit `f7efcc2`). Zero violations on 1,450 vanilla-FFA replays across 58 weekly buckets at `--per-bucket 25`. See `replay-parser/replay_parser/invariants.py` and `replay-parser/scripts/sweep_invariants.py`.

Note on the check shape: post-flip-on-general-tile is intentionally weakened to "captured player no longer owns gt" rather than "captor owns gt". Same-timestep chained captures and same-timestep normal-combat recapture of a just-flipped tile both happen in real games and aren't derivable from the event list alone. Comment in `invariants.py` covers this.

**Next: fixture-based regression tests.** Full spec in [`docs/2026-05/5.14-4-sim-rust-port-complete.md`](docs/2026-05/5.14-4-sim-rust-port-complete.md) under "Fixture-based regression tests — design landed, build queued":
- 5–8 hand-picked replays covering distinct sim corners (snowball win, multi-capture, AFK-heavy, neutralize edge, max-timestep, multi-way kill).
- Per replay: `wire.bin` + `expected.npz` with snapshots at `[0, T/4, T/2, 3T/4, T-1]` + end-state vectors + event lists + damage matrices.
- `tests/regen_fixtures.py` for deliberate regeneration. Discipline rule: understand *why* fixtures fail before regenerating.
- One pytest file parametrized over fixture IDs, in the deleted `test_state.py` slot.
- Estimated ~150–250 KB total, ~100 lines of code.

**Useful tool while selecting replays:** `replay-parser/tools/timestep_viewer/` renders any replay's per-timestep state as a self-contained HTML file for side-by-side compare against `generals.io/replays/{id}`.

---

please carefully get oriented and then check-in with me. we will discuss and collaboratively refine the plan before implementing.
