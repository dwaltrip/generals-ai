# Cloud bring-up — picking up at step 2

We're mid-bring-up of BC training on Modal. Step 1 (CPU smoke — image builds + training stack imports inside the container) is done. Next is step 2: corpus Volume + smoke slice.

**Branch:** `cloud-real-mvp` 

See recent history: `git log --oneline --decorate -20`

**Read first:**
- `docs/2026-05/5.22-2-cloud-real-mvp.md` — running tracker. Status, findings, next step (step 2 sub-tasks + open questions), remaining sequence.
- `docs/working-with-modal-cloud-gpu.md` — evergreen Modal reference. Image pattern (`add_local_python_source`), lockfile workflow with `--no-emit-workspace`, native-call entry shape.

**Code to skim:**
- `training/scripts/run_bc_modal.py` — what step 1 built; step 2 extends with Volume mount + read-smoke function.
- `training/scripts/run_bc_local.py` — local sibling, full CLI shape. The step-4 cloud entry will mirror this.
- `training/data/splits/probe_500.json` — natural smoke manifest.

**Modal Volume docs:** https://modal.com/docs/guide/volumes — `modal volume create/put/get` are the relevant CLI ops.

**Other references (read or search through as needed):**

- `docs/2026-05/5.22-1-training-dep-audit.md` — dep graph audit; useful if debugging install issues
- `docs/2026-05/5.21-9-cloud-poc-and-modal.md` — kickoff doc for the spike (superseded by 5.22-2 but has historical context)
- `tools/regen_modal_reqs.sh` + `.githooks/pre-commit` — modal_requirements regen flow (auto-runs on uv.lock changes)
- `cloud-poc/modal_entry.py` — the original POC entry; still in tree, decision to keep-or-delete deferred

---

# Getting started

Check in with me once oriented. The tracker doc has step 2's sub-tasks laid out. But please pause to discuss + align before we begin.
