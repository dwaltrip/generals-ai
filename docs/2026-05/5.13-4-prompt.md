# Intro

Continuing work on the replay parser / simulator (sibling subproject to `replay-collector/`). Located in `replay-parser/`.

## Session Goal

Continue debugging the mis-matches.

See output of `replay-parser/scripts/sweep_match_rates.py` located at `replay-parser/tmp/sweep_match_rates-20260513-2226.md`.

# Background + Documentation

**Doc freshness:** Docs haven't always been kept up-to-date. And the in-doc `Date:` / `Last updated:` headers may be out of sync as well. Run `uv run tools/docs_info.py` to get clarity on this. It provides file mtimes and an overview of git history.

## Primary context docs (read these carefully)

1. `docs/2026-05/5.13-3-session-notes.md` - Notes from most recent session where we figured out the first major mismatch issue.
2. `docs/2026-05/5.13-2-parser-session-handoff.md` - Notes / handoff from a few sessions ago where we actually did the replay parser implementation and then began debugging the initial discrepancies.
3. `docs/replay-parser-design.md` — parser + simulator design and rationale.

4. `docs/generals-io-game-mechanics.md` — rules reference. §6, §7, §9 are
load-bearing for the simulator.

## Key reference docs (lightly skim, read deeper as needed)

1. `docs/game-mechanics-appendix-resolved-ambiguities.md` — appendix for game mechanics doc with bundle line refs for previously-ambiguous mechanics.
2. `docs/2026-05/5.11-2-bundle-reading.md` — JS-bundle trace + line refs.  **§3 "Net-new precision" is the high-value chunk for this session**: orchestration order (3.1), combat math (3.2), general capture (3.3), `lbSort` ranking (3.6), and initial-state population (3.7). This material isn't folded into any canonical doc yet.
3. `docs/replay-format.md` — `.gior` wire format reference (v18).
4. 

## Additional reference material — don't read on orientation; navigate to specific sections only when a question requires them

If you need to search through multiple of these docs, consider dispatching an `Explore` sub-agent instead. For a quick lookup in one doc, a targeted skim is usually fine.

- `docs/replay-parser-status.md` — semi-stale status doc (from before we did the first pass implementation of the parser + simulator)
- `docs/2026-05/5.11-1-session-notes.md` — filter-counts report session. The empirical numbers behind §4 (filter survival), §5 (perspective yield), §8 (sample volume).
- `docs/compute-considerations.md` — source of §8 sample-volume math (the design doc inherits from it). Original numbers were against the 135k corpus; 5.11-1 has the updated ~197k figure.
- `docs/network-architecture-design.md` — §4 specs the action space (`[pass + 4 directions × 2 splits]` per cell); named as a parser dependency in design doc §7.
- `docs/observation-tensor-design.md` — obs-channel layout. Per design doc §3 the parser doesn't materialize obs tensors, but its intermediate output has to feed them.
- `docs/assumptions-and-key-calcs-grab-bag.md` — cross-cutting reference with stable item IDs (`A.A2` = filter survival, `A.A4` = board-symmetry
multiplier). Useful when deep in the numbers.
- `docs/2026-05/5.02-4-initial-mvp-sketch.md` — high-level Phase 1 framing.
- `research/gior-format/generals-main-prod-v31.4.1-d51b92c0.js` — live JS bundle (~136k lines).

---

# Next steps

Once you are oriented, check-in with me and let's discuss what to do next. This will likely be a collaborative debugging + investigative session.
