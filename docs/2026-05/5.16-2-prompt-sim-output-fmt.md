# Background

So the replay-parser is almost completely done.

The main thing remaining is to actually produce the intermediate output format and persist it for the training pipeline.

Recent work:

- Finished doing timestep level validation of the sim.
    - Wrote tests for internal invaraints.
    - Built a timestep viewer that I used to very carefully validate several replays by hand against the official generals.io replay viewer.
- Added integration tests for the sim, using fixture data generated from our DB. This should help prevent regressions.

Note: The status and design docs for the replay-parser are a bit out of date but still have a lot of good context.

# Session Goal

I'm thinking that in this session we tie off the replay-parser and simulator. Let's implement the v1 output format.

Then, in coming sessions, we can work on the major mising pieces needed for actual training runs:

- Initial v1 implementation of the actual neural net model
- Lock in final decisions for the v1 observation tensor
- Minimal training pipeline and related infra
- Any lingering training methodology design choices - e.g. hyperparams, etc.
- Test locally with CPU and hopefully apple silicon GPU
- Minimal test run with GPU provider (probably modal.com or something similar)

# Context

## Key docs (ready carefully)

- `docs/replay-parser-status.md`
- `docs/replay-parser-design.md`
- Section 9 and Section 10 of `docs/2026-05/5.13-1-parser-implementation-plan.md` - 
    - Section 10: Output intermediate format
    - Section 9: Action-stream recording
    - Grep for "## N."
- `docs/2026-05/5.02-4-initial-mvp-sketch.md`

## Additional references (skim lightly or search through)

- `docs/generals-io-game-mechanics.md`
- `docs/network-architecture-design.md`
- Observation tensor docs:
    - `docs/observation-tensor-design.md`
    - `docs/2026-05/5.05-1-observation-tensor-design-part2.md`
- `docs/replay-format.md`

## docs_info.py tool

Run the docs info tool to get an idea of the state of the docs.

```bash
uv run tools/docs_info.py -l 30 -c 2
```

## Look through the code

Key entry points:

- `replay-parser/replay_parser/parser.py`
- `sim-core/src/lib.rs`
- `sim-core/python/sim_core/_native.pyi`

The above lists of docs and context may not be fully comprehensive, but it's definitely a good starting point.

# Next steps

Once you are feeling oriented, please check in with me. Let's work together to figure out what we need to do.
