# Handoff — train.py refactor, Parts 5–6 (`5.28-7`)

**Status:** Parts 1–4 done. PRs 1–3 prior sessions; 4a/C1/4b this session (`e46aa63`, `50cef2d`, `208644b`, `5930423`, `6c915e6` on `train-py-refactor`). Tree clean.

### Required reading
1. `docs/2026-05/5.28-2-train-refactor-and-resume-design.md` — the contract (§`--legacy-lr-warmup-batches`, §Resume-side loader, §Sub-PR #5/#6).
2. `docs/2026-05/5.28-4.4-train-py-refactor-notes-part-4.md` — esp. the `prepare_resume` **cross-PR note for PR 5**.
3. `docs/2026-05/5.28-6-train-py-refactor-session-notes.md` — deviations / out-of-scope / testing gaps from Part 4.

### Start here: Part 5 (legacy resume + LR warmup)

Sketch first (`5.28-4.5`, per the workflow), then implement:

- **`WarmupSchedule`** in `bc/resume_warmup.py` — linear ramp `target_lr·(t+1)/N → target_lr`, then holds; counter transient on the object, spans the resumed segment's batches (not per-epoch). `target_lr = effective.lr`.
- **`--legacy-lr-warmup-batches N`** — **confirm first:** I plan to make this a CLI directive (like `--resume`), *not* a `TrainConfig` field (see session-notes deviation #3). Threaded to `bc_resume` → `run_training(warmup=…)` → `train_loop` → `train_one_epoch` (calls `warmup.step(optim)` at batch top). `warmup=None` default ⇒ fresh path is a no-op.
- **`is_legacy_checkpoint`** on `ResumeInfo` + detection in `prepare_resume` (combined = dict with `"model"`; legacy = bare state_dict).
- **Legacy without the flag → abort** (cold-restart is opt-in).
- **Legacy epoch fix** (the cross-PR note): legacy ckpt has no `epoch` ⇒ `from_checkpoint` falls back to 0, which is wrong — set `state.epoch = info.parent_epoch` (from the filename) for legacy.

**Verify:** fresh no-op via `./training/scripts/smoke_train.sh` (warmup=None path); `WarmupSchedule.step` formula unit test; `is_legacy` detection + abort tests; a synthetic legacy-resume e2e (save a bare `state_dict`, resume with the flag, assert epoch set from filename + it trains).

### Then: Part 6 (operational)

Resume the `runs-cloud/2026-05-27T…` 5/8 run with `--legacy-lr-warmup-batches 500` to finish the 3 missing epochs. **Needs your go-ahead** — it's a real cloud run + `modal volume` ops (the Modal resume path is untested locally; first real exercise). Pause before any remote upload.

### Watch-fors

- Local MPS `run_val` crash → `--skip-val` for local smoke (cloud/CUDA fine).
- `test_single_tick_inference` is skipped. pre-existing, we can leave it.
- Working mode: collaborative/discussion - first default; roll-through is opt-in per bundle.

---

# Getting started

Please carefully and thoroughly orient yourself. Take a peek at the relevant code.

Once you are ready, check in with me.
