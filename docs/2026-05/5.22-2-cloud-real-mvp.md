## BC training on Modal — bring-up tracker

**Date:** 2026-05-22
**Branch:** `cloud-real-mvp`
**Status:** Step 4 (T4 GPU smoke) complete. Hit a data-pipeline bottleneck — DataLoader detour planned before step 5 (H100 trial).

This doc tracks the remaining steps to land BC training as a real Modal entrypoint. Picks up from [`5.21-9`](./5.21-9-cloud-poc-and-modal.md) (which was the cloud-poc kickoff — now done) and [`5.22-1`](./5.22-1-training-dep-audit.md) (training dep audit, also done).

## Where we are

- `training/scripts/run_bc_modal.py` — Modal cloud entrypoint. Full shape: `train` local-entrypoint extends `build_arg_parser` with `--modal-gpu` (required); `train_remote` `@app.function` (T4 default, both Volumes mounted) runs `initialize_run_dir` → `_write_args_cloud` → `bc_run`. Pull-back command printed after return.
- `training/scripts/smoke_modal_prototyping.py` — earlier `smoke` / `smoke_volume` / `smoke_outputs` functions, renamed to mark them as exploratory/scaffolding. Kept for regression smokes.
- `training/scripts/run_bc_local.py` — local sibling. Calls `initialize_run_dir(config)` then `bc_run(config)`.
- `training/scripts/build_cloud_slice.py` — manifest → self-contained slice (sim + meta + manifest) builder, for `modal volume put`.
- `bc/train.py` — runner. Exposes `initialize_run_dir(config)` (mkdir + args.json + announce) and `bc_run(config)` (precondition: run_dir exists). `bc_run` does not know how the dir got there.
- `bc/train_config.py` — `TrainConfig` (frozen dataclass with `run_dir: Path` as the full per-run path) + `make_run_id()` + `json_default`.
- `bc/train_cli.py` — shared parser. `--manifest`/`--intermediate`/`--out-dir` all non-required at argparse level; `config_from_args` validates post-parse.
- **Volumes on Modal:** `generals-ai.parsed-replays` (RO inputs, ~23 MB probe slice), `generals-ai.training-runs` (RW outputs).

## Findings worth carrying forward

- **`add_local_python_source` beats `add_local_dir + uv_pip_install("/pkg")` for pure-Python packages.** Modal resolves the modules via the local-env import path and drops files at `/root/` (on PYTHONPATH). No synthetic install paths, no pyproject re-resolution, no hatchling build. Use the pip-install path only if a package needs entry points / console scripts / build hooks (none of ours do).
- **`uv export` needs `--no-emit-workspace`.** Without it, workspace cross-deps appear as `-e ./packages/<name>` in the requirements file. Modal's `uv_pip_install(requirements=...)` runs before any `add_local_*` layer, so the path doesn't exist in the container → image build fails. Documented in [`working-with-modal-cloud-gpu.md`](../working-with-modal-cloud-gpu.md).
- **The dep audit was right about what `training` pulls in:** torch + numpy + matplotlib + utils. The earlier `replay-collector` transitive bloat (from `utils.player_name_lists` → `replay_collector.usernames`) is gone since the `usernames` module moved into `utils/`.
- **`pick_device("auto")` now prefers CUDA → MPS → CPU** and `pick_device("cuda")` raises if CUDA isn't available — silent CPU fallback would have been a footgun on a paid GPU box. `--device auto` from the cloud wrapper does the right thing.
- **Manifests are already cloud-portable.** `samples_for_split` takes `intermediate_root` from the caller, not from the manifest's recorded `intermediate_root` field (which is informational). Step 2's anticipated "path translation" problem dissolved — the cloud function just calls `samples_for_split(m, "train", Path("/data/intermediate"))` and IDs resolve under the Volume mount.
- **Volume naming convention.** `generals-ai.<role>` as a pseudo-namespace inside the default environment. Inputs Volume is `generals-ai.parsed-replays`; outputs Volume (step 3) follows the same pattern. Real Modal environments are overkill for one user + one app.
- **Slice helper.** `training/scripts/build_cloud_slice.py` builds a self-contained slice (`sim` + `meta` + manifest) from any manifest. ~23 MB / 868 files for the probe_500 slice; trivial to upload. Re-usable for any future cloud slice.
- **`bc.train_config` extracted.** `TrainConfig` + `make_run_id()` now live in `bc/train_config.py`. `TrainConfig.run_dir` is the full path to *this* run's directory (was `out_dir`, the parent). Wrappers compose `run_dir = args.out_dir / make_run_id()`. Run-id format is UTC `YYYY-MM-DDTHH-MM-SSZ`, identical local + cloud.
- **Auto-commit covers single-shot writes.** Writes inside a Modal function become visible on the Volume on clean function exit — no `vol.commit()` needed for step 3. Mid-run commits will matter for long training runs (step 5+) when we want to pull intermediate checkpoints.
- **`modal volume get` gotcha.** With a non-existent local destination (or trailing slashes), the CLI collapses recursive contents into a single output file. Always `mkdir -p` the target first and drop trailing slashes. Documented in [`working-with-modal-cloud-gpu.md`](../working-with-modal-cloud-gpu.md#gotchas-worth-knowing).
- **Extended-parser beats two-parser.** Cloud-only flags (`--modal-gpu`) `add_argument`'d to the *same* parser as training flags. Single source of truth + collisions raise `ArgumentError` at startup (explicit, not silent). Two-parser `parse_known_args` would silently steal future training flags from the operator.
- **`initialize_run_dir` extracted from `bc_run`.** Cloud entry can drop `args_cloud.json` into the run dir *before* training starts. `bc_run`'s precondition: `config.run_dir` must already exist; error message reports the failure, not the fix (callers may init the dir however).
- **`args_cloud.json` confirmed `Tesla T4` at runtime.** The `cuda_device_name` field is the load-bearing one — confirms Modal honored the requested GPU class. Worth grepping in retrospect if a future run reports a fallback variant.
- **T4 was 1.6–3.3× slower than M1 at this scale.** Data-pipeline bottleneck (Volume reads + `num_workers=0` DataLoader + no `pin_memory`), not GPU compute. T4 epoch-2: 22 sps train / 177 sps val. M1 epoch-2: 70 sps train / 284 sps val. The val-faster-than-train shape is present locally too (IterableDataset cost), but amplified on cloud by network FS. Real epochs (10k+ batches) would amortize the cold-cache cost across reads.

## Next: DataLoader detour before step 5

Goal: validate that the bottleneck is data-starvation by adding minimal DataLoader knobs and re-measuring. If throughput jumps significantly, the bottleneck hypothesis is confirmed and we can move to step 5 (H100). If it barely moves, the network-FS hypothesis is strong and we look at staging to local SSD before step 5.

Sub-tasks:

1. **DataLoader changes** (both train in `bc/train.py` and val in `bc/eval.py`):
   - `num_workers=1` — single worker = safe with current `IterableDataset` (no sharding yet; see below). Buys file-read / GPU-step overlap via prefetch.
   - `pin_memory=(device.type == "cuda")` — async H2D on cloud, no-op on M1/cpu.
   - `prefetch_factor=2` — explicit even though it's the default.
2. **Re-run local + cloud with bigger workload:** `--batch-size 128 --max-batches 20 --epochs 2`. Bigger batch better amortizes per-step overhead; 20 batches warms the page cache for a closer-to-steady-state read.
3. **Compare to baseline.** Baseline numbers above. Decide: proceed to step 5 (if data starvation fixed) or stage-to-SSD (if not).

### Backlog after the detour

- **`IterableDataset` worker sharding.** Currently `num_workers > 1` would yield each sample N times (docstring is explicit). Implementation: `__iter__` checks `torch.utils.data.get_worker_info()` and shards `self._groups` modulo worker count, plus mixes worker_id into the shuffle seed. ~15-20 line change, but touches per-epoch shuffle semantics — wants a test.
- **Stage to local SSD pattern.** If network reads are the residual bottleneck post-DataLoader, the next move is `cp -r /data/intermediate /tmp/intermediate` in `train_remote` before calling `bc_run`. ~30s upfront, then reads are effectively free. The intermediate path becomes operator-supplied via a new cloud-side flag or env var.
- **Doc fix:** `working-with-modal-cloud-gpu.md`'s "Native vs shell-out" example still shows the two-parser pattern. Should be flipped to extended-parser to match what we shipped.

## Remaining sequence

| Step | What | Status |
|---|---|---|
| 1 | CPU smoke (image builds, imports work) | ✅ done (commit `d580a39`) |
| 2 | Corpus Volume + read smoke | ✅ done |
| 3 | Outputs Volume + write/pull-back | ✅ done |
| 4 | T4 smoke — single training step on GPU | ✅ done (works; throughput data-starved) |
| 4.5 | DataLoader detour: num_workers + pin_memory + bigger batch, compare local vs cloud | next |
| 5 | H100 trial — single small epoch | after 4.5 |
| 6 | First real Cluster C run — 50k+ games × 10–20 epochs | |

## Other deferred items

- **Doc update for `working-with-modal-cloud-gpu.md`.** ✅ updated this session to reflect the `add_local_python_source` pattern, `--no-emit-workspace` flag, and the corrected native-call shape (the prior example referenced an import path that never worked).
- **`cloud-poc/` survival.** Still in-tree. Can stay as a "does Modal still work" smoke or be deleted once the real entrypoint is mature. Defer the decision until step 4+ lands.
- **Matplotlib in cloud image.** Declared as a `training` dep but only used by post-hoc analysis scripts. ~30 MB; not worth moving to an optional extra until image-size becomes an issue.

## Related

- [`working-with-modal-cloud-gpu.md`](../working-with-modal-cloud-gpu.md) — evergreen Modal reference (image pattern, lockfile workflow, native-call shape, gotchas)
- [`5.21-9`](./5.21-9-cloud-poc-and-modal.md) — cloud-poc kickoff; superseded by this tracker
- [`5.22-1`](./5.22-1-training-dep-audit.md) — training dep audit (done)
- `training/scripts/run_bc_modal.py` — Modal entry (full shape: extended parser, `train_remote` on GPU, args_cloud.json)
- `training/scripts/run_bc_local.py` — local sibling entry (full shape)
- `tools/regen_modal_reqs.sh` — `modal_requirements.txt` regen
- Baseline run dirs for comparison: `training/data/runs/2026-05-22T19-53-51Z/` (M1) and `tmp/cloud-runs/2026-05-22T20-49-34Z/` (T4)
