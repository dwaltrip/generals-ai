# Handoff — StaticMap follow-ups (`5.30-2`)

**Status:** The `static: Any` → `StaticMap` split landed in `7b65d2e` on `batch-run-game` (tree clean). Two optional follow-ups remain, both independent and low-risk. Neither blocks anything.

### Context (where things landed)
- `game_types.StaticMap` — the typed board spec `sim_core.new_state` consumes (9 fields: dims, usernames, mountains, initial cities / city-armies / generals / neutrals / neutral-armies).
- `replay_parser.GameStatic` **composes** it: `map: StaticMap` + replay-only `id`/`version`/`stars`/`modifiers`.
- Game path carries a `StaticMap` (unwrapped at `seed_map.load_static_from_db` → `.static.map`). Rust `build_state_from_static` (`sim-core/src/lib.rs`) reads the map's attributes; `simulate` reaches it via `replay.static.map`, `new_state` gets it directly.

---

### Follow-up A — `#[derive(FromPyObject)]` in `lib.rs`

Collapse the 9 repetitive `getattr().extract()` lines in `build_state_from_static` (`sim-core/src/lib.rs:14–25`) into one typed struct + one `.extract()`:

```rust
#[derive(FromPyObject)]
struct StaticMapInput {
    map_width: usize,
    map_height: usize,
    usernames: Vec<String>,
    mountains: Vec<i32>,
    initial_cities: Vec<i32>,
    initial_city_armies: Vec<i32>,
    initial_generals: Vec<i32>,
    initial_neutrals: Vec<i32>,
    initial_neutral_armies: Vec<i32>,
}
// then: let m: StaticMapInput = map_data.extract()?;  (num_players = m.usernames.len())
```

- **Why:** centralizes the field list as the Rust-side mirror of `StaticMap`; fewer stringly-typed lines. **Maintainability, not perf** — this is startup code (once per game / parse), so the `getattr` cost is irrelevant. Keeps the duck-typed boundary (the derive defaults to attribute access, so `GameStatic.map` / `StaticMap` / mocks all still work).
- **First, resolve the parked concern:** there was an unverified worry about a PyO3-version issue with the derive. We're on `pyo3 = "0.28"` (`sim-core/Cargo.toml`), where the derive is well-supported — so write a throwaway `.extract::<StaticMapInput>()` and `cargo build` to confirm before refactoring, rather than trusting the recollection.
- **Verify:** `uv run pytest replay-parser/tests/test_step_tick_parity.py` (exercises both `new_state` and `simulate` through this helper). `uv run` auto-rebuilds the `.so` via the `**/*.rs` cache-key.
- **Don't** make `StaticMap` a Rust `#[pyclass]` — that inverts ownership (type would live in sim-core, dragging a sim-core dep into game-types/replay-parser) and kills the mock interop. The duck-typed boundary is deliberate.

---

### Follow-up B — retire the `view_game.py` `SimpleNamespace` mocks

Existing TODO at `packages/eval-tools/scripts/view_game.py:27`. `_load_game` reconstructs two duck-typed mocks from a saved `.npz` (untyped, silent on field-name typos).

- **Easy half (do this first):** the *static* mock (`view_game.py:103–111`) → a real `game_types.StaticMap`. The npz already persists `initial_neutrals` / `initial_neutral_armies` (`save.py:25–26`), but `_load_game` currently omits them — load those two and construct `StaticMap(...)`. No back-compat risk: `save.py` has written neutrals since its first commit, and `view_game` only reads `save.py` output, so no on-disk `.npz` lacks them.
- **Harder half:** the *state* mock (`view_game.py:90`, a `SimpleNamespace` with `snapshots_*`, `*_events`, `alive`, `timestep`, …) needs its own dataclass(es) — the TODO names `SavedGameState` + `CaptureEvent`/`DeathEvent`. Match the read surface of `extract._build_payload` (`replay-parser/tools/timestep_viewer/extract.py`) plus what the viewer touches.
- **Once both are real:** `game_runner.viewer.render_viewer_html` can drop its `static: Any` / `state: Any` escape-hatch types. Note its wrapper is `SimpleNamespace(static=SimpleNamespace(map=static))` — a real `StaticMap` flows through that unchanged (it's the `.static.map` the shared `_build_payload` reads).
- **Verify:** `uv run pytest packages/self-play/tests/test_viewer.py` and render an actual saved `.npz` via `./packages/eval-tools/scripts/view_game.py <game.npz>`.

---

### Watch-fors
- The shared `_build_payload` is reused by both the replay timestep-viewer (real `ReplayData`) and `game_runner.viewer` (a faked `replay`). It reads `replay.static.map` — keep both callers feeding that shape.
- Pre-existing lint debt in touched files is left as-is (e.g. `nn_agent.py:27` I001, `replay_collector` TID251 in self-play tests) — not part of this work.
- Working mode: collaborative / discussion-first by default.

---

# Getting started

Please orient yourself carefully — read `7b65d2e` and peek at the files above before touching anything. Both follow-ups are independent; pick either. Check in with me once you're ready.
