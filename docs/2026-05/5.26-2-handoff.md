Here's the handoff:

---

# Eval Bot Session Handoff

**Date:** 2026.05.26

**What happened last session:** Implemented all of milestone 2 — WorldModel/PlayerView refactor, ThreatWindow + IncursionThreat, Defend gate, gather_path primitive, explore upgrade, spent detection, plan lifecycle. Opus review found and we fixed two spent detection bugs + army estimate gap. See `docs/2026-05/5.26-1-session-notes.md` for detailed process notes and architectural decisions.

---

## Required reading

1. `docs/2026-05/5.26-1-session-notes.md` — session notes (process, architectural decisions, what prompted them)
2. `docs/2026-05/5.25-1-eval-bot-spec.md` — behavioral spec (source of truth)
3. `docs/2026-05/5.25-4-m2-implementation-plan.md` — implementation plan (has stale sections marked TODOs, see session notes for details)
4. `docs/generals-io-game-mechanics.md` — game mechanics reference

---

## Next up: Milestone 3 — Kill-shot + Attack

From the original handoff:
- Kill-shot + Attack — the offensive gates
- ThreatScore/TargetScore, the kill-shot race condition
- Mostly wiring — the machinery (gather_path, BFS, world model) is proven
- TBD tuning values (initial defaults can be guesstimates)

### M3 extension points identified by Opus review

- `_is_spent` needs KillshotPlan-specific condition (attacking army < target general's army)
- Preemption check needs `not isinstance(plan, (DefendPlan, KillshotPlan))`
- AttackPlan and KillshotPlan variants already sketched in plan.py (future stubs)

### Other notes / Deferred items

- Expand randomization (spec §4.3 says random, code uses farthest-from-general)
- Impl doc cleanup (stale sections in 5.25-4)
- Testing: smoke test is minimal, detailed unit tests needed before tuning
- Warning-as-invariant pattern should be extended to more helpers

---

## Non-obvious context

- PlayerView carries fog-filtered arrays (`np.where(vis, god_state, sentinel)`). This is an architectural choice, not just convenience — downstream code doesn't need explicit vis filtering.
- `_tick_active_plan` only clears or emits, never creates plans. Re-evaluation clears and falls through to the decision ladder.
- `mobile_army` is a generic helper (army tiles can contribute). General's split contribution is caller-side.
- `threat_windows` dict is passed by reference into frozen PlayerView — works for now but consider a defensive copy if mutation risk grows.

---

After you are oriented, let's check in and discuss next steps. We will work collaboratively.
