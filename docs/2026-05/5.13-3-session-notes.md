# Session Notes

## Investigation: the v30.9.2 rule change

We started by reproducing the 57% ranking-match issue on `YC-ZjiMiY` that the previous session's handoff doc framed as a sim bug. You noticed the **replay viewer shows our ranking, not the listings** — which inverted the framing entirely. Then we converged on the real story over several iterations:

1. Initial hypothesis (kill partition is recent) — partly right, but obscured by the v30.6.10 changelog entry about "attacked anyone" (red herring; that was about star-awarding, not lbSort).
2. Cohort test with 2-day buffer around 2025-11-29: BEFORE 50% / AFTER 100% — flipped from what no-buffer showed, confirming a sharp rule change at v30.9.2.
3. You pulled the wayback v30.8.5 bundle. Found the **pre-patch lbSort had no has_kill partition at all** (line 23586). That's the simpler explanation: pre-v30.9.2 listings used alive/death-order/army/tiles/slot only.
4. Bisected the deploy time using the corpus itself: last OLD replay at `2025-11-30 05:27:21 UTC`, first NEW at `05:59:41 UTC` — a 32-min ambiguity window. (You noted this lines up with the 2025-11-29 changelog date in PT.)

## Code changes

**Commit `ad03001` — Version-aware lbSort**
- `validator.py`: added `partition_kill_no_kill` flag to `deduce_ranking` (default `True`), plus `deduce_ranking_for_replay(state, started_ms)` helper that picks the rule by timestamp and raises for the 32-min gap.
- `PRE_V30_9_2_CUTOFF_MS` / `POST_V30_9_2_CUTOFF_MS` constants pinned to the boundary replays (ms-precise).
- `scripts/find_lbsort_deploy_time.py` — the bisect tool, kept around for reproducibility.
- Added the v30.8.5 wayback bundle for the historical lbSort reference.

**Commit `bbc4848` — Army-write helpers + overflow handling**
- A sweep hit `OverflowError` on int16 armies (38670 > 32767). Design doc says skip such replays via assertion, but we hadn't wired it up.
- Added `set_army(state, target, value)` primitive and `increase_army(state, target, delta)` convenience wrapper in `state.py`. Both check overflow → raise `ArmyOverflowError`.
- Routed **every** armies write in combat + production through one of these. No raw `state.armies[...] = ...` left during simulation.
- New `errors.py` with `ArmyOverflowError`.
- Extracted `is_vanilla_ffa` to `replay_parser/_shared.py` with per-filter comments + TODO about the overlap with `replay_collector.sql_helpers.ffa_match_filter`.

**Commit `3bfdd93` — Sweep with random sampling + error logging**
- `scripts/sweep_match_rates.py`: 100/week random sample with fixed seed, pre-fetches blobs only for sampled IDs, chunked IN clauses.
- Try/except around `decode_wire` and `parse_replay` — counts `overflow`/`parse-err` per bucket, samples first 20 errors in report tail, streams to stderr.

## Sweep result

5,800 replays across 58 weekly buckets (Mar 2025 → May 2026):
- **Pre-cutoff: 100.0% match every week.** Validates death tracking, alive/dead, army/tiles, and ordering logic in our simulator.
- **Post-cutoff: 85–95%, averaging ~92%.** Consistent across months — not a regression, just a steady ~8% mismatch.

Since the only thing differing between rules is `has_kill`, the post-cutoff signal is **localized to has_kill computation**. That's the obvious starting point for the next investigation.

Report: `replay-parser/tmp/sweep_match_rates-20260513-1959.md`

## Open items for next session

1. **The has_kill investigation** — drill into post-cutoff mismatches. Strong candidates: capture-by-surrender, captures during another player's AFK countdown, surrender + last-attacker credit.
2. **Two parse errors** in the sweep — `stars=None` (`wire[5]`) in two replays. One-line fix in `decode.py`.
3. **Parser performance** — ~36ms/replay after the set_army refactor (~2.5× slowdown). Easiest win is a scalar fast path in `set_army` that skips numpy for Python int values. We're deferring this; full corpus sweep at current speed is ~100 min.
4. **Integration test assertion** — we deferred picking a threshold until we saw the sweep data. Now we have it: 100% pre-cutoff is a tight assertion candidate; post-cutoff would need a threshold (~85% floor) until has_kill is fixed.
5. **Task #10** from the previous handoff (end-to-end ranking-match test) is unblocked by these findings.

Branch `start-replay-parser`, working tree clean. Three new commits since session start: `ad03001`, `bbc4848`, `3bfdd93`.
