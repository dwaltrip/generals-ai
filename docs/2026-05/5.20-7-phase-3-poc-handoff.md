# Phase 3 — PoC handoff

**Written:** 2026-05-20 (for next session, 2026-05-21+)

**Branch:** `spike-phase-3`

**Scope:** Critical-path gate is closed. Next session picks up from the overnight PoC run output. [`5.20-5`](./5.20-5-phase-3-status.md) is the decisions / open-questions index.

# What landed this session

- **Chunks 4 + 5 complete.** Checkpointing (state_dict only, keep-all, inside the run dir) + per-epoch val eval (`bc/eval.py::run_val` — losses, top-1/top-3, pass_acc, pass_frac, 8-bucket action histograms). Critical-path-to-loss-curve gate closed.
- **Throughput probe** at 100 games × 1 epoch: ~170 samples/sec train, ~287 val, val/train wallclock ~3% at batch=64 on M1 MPS. *Tentative — single 4-min run, per-batch rate varies.*
- **Curated-player filter wired** into `bc/filters.py::eligible_perspectives` as defense-in-depth. Parser already filters at parse time, so this is a no-op against the live corpus. Hook preserved for future "train on a subset of the curated list" without re-parsing. There's a TODO at the call site about revisiting.
- **Player-list loaders moved to `utils.player_name_lists`.** Cross-workspace migration. `filter_valid` stays in `replay_collector.usernames`; `utils` imports back across the dep direction as a known compromise (TODO at the import site, also visible in `utils/pyproject.toml`'s `replay-collector` dep).
- **Train-loop refactors** before chunk 5: `train_one_epoch` extracted from `main`; `_build_arg_parser` + `run(args)` split from `main`; `flatten_policy_logits` extracted from `bc_loss`.

# Going-to-bed plan (what the user actually did)

The user planned `(a) → (c)`:
- **(a)** Tighter throughput probe: 500–1k games × 1 epoch (~20–40 min, attended).
- **(c)** Full PoC: 1–2k games × 3–5 epochs (3–7 hrs, overnight via `caffeinate -i ...`).

So when next session opens, expect to find a recent `training/data/runs/<TIMESTAMP>/` from (a) and another (newer) one from (c). The most recent one is (c) — that's the one to inspect.

# Docs to read first

- [`5.20-5`](./5.20-5-phase-3-status.md) — primary index. Throughput open question now has data; success-bar question has a "pipeline-works" data point + caveats.
- Most recent `training/data/runs/<TIMESTAMP>/epochs.jsonl` — the PoC output.
- `git log --oneline -n 10`

# Working style

Same as [`5.20-6`](./5.20-6-handoff-prompt.md) — chunk-per-commit, `pytest training/tests/` green at start + major commits, `uv sync` from workspace root only, status doc decisions log gets only durable choices.

# Next steps

1. **Check the PoC run first thing.** Did it complete? Did it crash mid-epoch? Inspect `epochs.jsonl` row count vs `args.epochs`.

2. **Read the curve qualitatively** before judging "good" or "bad":
   - Loss components descending across epochs? Or plateau / bounce?
   - top-1 / top-3 trajectory — should rise epoch-over-epoch on real data.
   - `action_dist` vs `action_target_dist` — still tracking, or collapsing onto one direction?
   - Later-epoch samples/sec — steadier than epoch-1's MPS-warmup-mixed rate.

3. **Decide direction from what (c) shows:**
   - **Healthy:** scale up (more games, more epochs), OR push into chunk 6 (shuffle-buffer, med-high uncertainty) / chunk 7 (board-symmetry aug).
   - **Pathological** (mode collapse, flat curve): triage. Shuffle-buffer is the most likely fix if in-game frame correlation is the cause.
   - **Throughput-bound** (epoch took too long): multi-worker dataloader is the first lever — open question in the status doc.

4. **Don't sleep on the open questions.** The Phase 3 success-bar question is still open; the PoC numbers are the first input to that. Multi-worker is in the same neighborhood.
