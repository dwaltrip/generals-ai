# Handoff — goldens pre-build (items 1 and 2)

*Date: 2026-07-12. Session handoff for the [behavior-goldens first cut](7.12-1-behavior-goldens-first-cut-v2-overview.md) pre-build work. Deliberately slim: state lives in the docs this note points to.*

*Update 2026-07-13: next step 1 (apply item 1) is done — see [7.12-3](7.12-3-pre-build-item-1-investigation.md)'s update line. The next session starts at step 2, the size-and-runtime probe, so the reading list's first two bullets (the item-1 material) no longer apply.*

## State

- [7.12-3](7.12-3-pre-build-item-1-investigation.md): pre-build item 1 investigated, with a consolidated edit plan and proposed draft texts. **Applied 2026-07-13** (see its update line).
- [7.12-4](7.12-4-event-class-checklist.md): the first pass of pre-build item 2, the event-class checklist. Its §2 records the revised posture on the codified tags: comprehensive tags ship with the build, and trimming is a fallback.
- Both landed across two commits on `behavior-goldens-pt1` (local, unpushed at handoff time).

## Decisions made after those docs landed (recorded only here)

- **Item 1 is applied before or early in the build**, not deferred to after it. Three reasons:
  - The stale banner and channel-count trails sit in exactly the files the build reads hardest.
  - The banner's alive-mask claim is false rather than merely stale.
  - The capture-seam documentation was sequenced to precede first bless by design (7.12-2 §2 item 4).

  Deferring would have bought only better cross-references in the rewritten banner (pointers to real goldens artifacts instead of design docs). A cheap post-build touch-up gets the same, without delaying the corrections.
- **The obs-channel sweep lands before the frozen-frame pick rule is implemented.** The sweep's second consumer is a review of that rule (7.12-4 §8), and the review has to happen before references are frozen under the rule. The sweep does not need to precede item 1 or the probe.

## Next steps, in order

1. **Apply item 1** from 7.12-3 §7's edit plan. The edits are mechanical and the draft texts are written. Two leans to confirm at application time: split the capture-seam paragraph to `step_memory`'s capture-event block, and drop the `_encode_army_delta` TODO.
2. **Pre-build item 3, the size-and-runtime probe**, is the designated first build step (7.10-2 §9 item 3, budget context in §4). It naturally doubles as the produce-fixtures skeleton: parse one real replay to npz, walk it, and measure compressed reference size and per-fixture walk time.
3. **Select fixtures** against the 7.12-4 tag list, via a corpus query over the parsed event arrays.

## Queued (no new urgency)

- The boundaries-ledger seeding (three retro May–June records, queued since 7.08-1).
- The second comprehensiveness pass on the 7.12-4 checklist.
- The `perf_report.py` and `get_obs_channel_indices` functional questions parked in 7.12-3 §2.
- The post-build banner cross-reference touch-up (above).

## Reading list for next session

Open with, in order:

- 7.12-3, the item-1 brief.
- [6.18-6](../2026-06/6.18-6-obs-alive-tick-seam.md) and [6.19-1](../2026-06/6.19-1-obs-player-status-channels-landed.md), item 1's doc-edit target and its evidence.
- 7.10-2 §9 item 3 and §4, the probe spec and its budget context.
- 7.12-1, as standing orientation.

On demand as the build progresses:

- 7.12-4, at fixture selection.
- [7.12-2](7.12-2-departures-from-7.10-2.md), where 7.10-2 and 7.12-1 disagree.
- 7.09-1 (comparison policy §6, flows §7), when regen tooling starts.
- [The confounds framework](../confounds-framework.md), when boundary-record work begins.
