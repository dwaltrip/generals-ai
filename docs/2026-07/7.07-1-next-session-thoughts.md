Next session thoughts and recommendations (copied from earlier Fable session)

## Recommended: one wrap-up session, in dependency order

**1. The obs-fix investigation first.** Three reasons it leads: it's the only potentially-live correctness item, its finding is an input to both other items (the changelog's seed entry, route-1's disposition for pre-fix checkpoints), and it's bounded — mostly git archaeology plus characterizing the delta, with the five steps already written down in 7.06-1. There's also a quiet clock on it: every eval comparison run before it's resolved is one more chance to cross the fix boundary unknowingly.

**2. The behavior-changelog build (~an hour).** Right after the investigation, while the finding is fresh — seeding the ledger with a real entry validates the format better than designing it empty. Includes the AGENTS.md line.

**3. The route-1 policy call.** The inventory pass (what checkpoints exist, what still loads them) plus the decision. Doing it after the investigation means the pre-fix-checkpoint disposition folds into the same policy statement instead of being a separate caveat. This closes bucket A entirely.

Then ckpt-config pauses and the session after that is core work — the aux-head/elim arc, with the v1 spot-check riding along on its first training run.

## Alternatives, honestly weighed

- **Straight back to core work, wrap-up items done opportunistically.** Defensible — nothing blocks, and you've said you want to get back. The cost is the obs-fix hypothesis staying unverified while new eval numbers accumulate, and route-1/changelog decaying from "warm context" to "reconstruct from docs." I'd at least pull item 1 forward even in this plan.
- **Route-1 first.** I considered it (it's the last bucket-A item and a pure decision), but deciding checkpoint support before knowing which checkpoints carry the obs confound gets the order backwards.

My overall lean: the full wrap-up session if the investigation stays small. Items 1–3 are plausibly a half-day together.

One more small thing for whichever session comes next: the two smoke checkpoints in `packages/training/tmp/smoke-runs/` from 6.28 are the only v1 artifacts around — worth leaving in place until the first real v1 run exists, since they're what any quick v1-path check loads.
