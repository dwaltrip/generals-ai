# Handoff

## Continue the argmin-toy surrender re-admission work

The design doc: `docs/2026-06/6.18-4-argmin-toy-surrender-design.md`

Read it first. It's self-contained: verified surrender state machine, the encoding × target grid, measured ceilings, per-cell predictions, risks, and the build order (§9). Also skim 6.18-3 (prior grid read) and the existing argmin_toy/ package.

## Start at §9 build step 1: 

- A committed, logged recomputation of the §5 ceilings (incidence 6.58%, surrender-subpop irreducible error 46.5% → ~53.5% none-ceiling) plus per-split surrender counts, on cloud_24k_sub_5k val.
- This is the de-risk gate — it validates the slice's premise (and confirms the held-out surrender subpop is big enough to read) before any model trains.
- The reproducing fq command is in §5.
- Dev on cloud_24k_sub_5k, not probe_500 (probe_500 overfits — 6.18-2).

## Key framing 

Prod's status channel is capture-based and blind to surrender, so for the argmin-over-alive target none ≡ capture_status (~53.5% ceiling) while a true alive channel reaches ~100%.

The toy's alive-vs-army>0 target fork proxies the elim-head's own target decision (next-DeathEvent vs next-board-removal).

The overall results of the toy-argmin spike will be significantly inform this decision, as well as any other changes we need to make to the elim-head.

## Working style

We are still in invesgitation mode. Please check-in regularly. I'd like to discuss any findings *as they come in* and before proceeding to major next steps or unplanned work that you are drawn towards. Small side-investigations, but don't go off a massive rabbit-hole without discussing.

Let me once you are fully oriented and have the lay of the land. Let's align on how to proceed.
