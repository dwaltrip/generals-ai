# Session goal

Can you look at the most recent 2 commits? and the relevant related code?

We need to come up with a registry system that keeps the CLI very ergonomic but doesn't put domain-specific code inside of `fq`.

The previous commits took a stab at removing the domain-specific logic (non-lib code) that was in the lib at: `fq.families` and `fq.derivers`.

Most of it is out, it is looking better overall. But it's still a little rough / unpolished. The patterns here may need some refinement.  And notably, the registry is now broken.

**Main goal:** Fix the registry, polish the library a bit more, and improve the ergonomics of using it.

Ergonomics needs:

1. Building scripts (one-offs and )
2. Running convenient one-off snippets as CLI call, similarly to `python -c "..."`

# Relevant session docs

For context, please take a look through these recent relevant docs (listed newer to older):

- `docs/2026-06/6.16-2-fq-join-dump-and-head-vs-rule-by-margin.md` - Follow-ups on `fq` + starting to actually use it to do analysis
-  `docs/2026-06/6.16-1-fq-mvp-build.md` - notes from the MVP build out of `fq`
- `docs/2026-06/6.15-3-fq-analysis-toolkit-design.md` - design doc for the new `fq` analysis lib

# Next steps

Once have the lay of the land, let's discuss, I'd like to hear your thoughts.
