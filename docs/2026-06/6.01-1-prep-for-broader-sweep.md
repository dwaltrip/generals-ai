
**WIP document***

## Tier-1 Questions:

1. Why do the channels with different N values in `CHANNEL_ORDER` in `constants.py` have hard-coded Ns? There are 2 types of Ns here: (1) num players (8 for FFA, more reasonable to hard-code) and (2) number timesteps in recent-dense-history
2. Easiest way to implement the `OBS_TENSOR_VARIANTS` below?
3. How hard is it to add *trunk dropout*? Is it valuable? What values are worth trying?
4. How does increasing contraction depth from 2 to 3 affect the model size?
5. (quickly) What values are worth considering for `shuffle_buffer_size`? This one change had a massive effect on the current value head (placement-prediction) actually working I believe we just picked the value of 2048 and called it a day, after just a brief discussion.


## Tier-2 Questions:

1. `5.20-3-model-architecture-implementation.md` says policy is "small". should we make it bigger? How does it compare to deepnash? how to think about it?


```python
    OBS_TENSOR_VARIANTS = [
        "DENSE_HISTORY_N=5",
        "DENSE_HISTORY_N=10",
        "DENSE_HISTORY_N=20",
    ]
```
