## Handoff 

We are continuing the obs-build / data-starvation investigation.

**Session goal:** Run the new timing profiler on-target (Modal H100) at n=5/10/20, read where the obs-build costs and other data-pipeline costs actually land, and investigate options for improving the situation.

**Read carefully:**

- `docs/2026-06/6.05-4-timing-profiler-infra.md` — new timing / profiling tooling.
- `docs/2026-06/6.05-3-obs-pipeline-starvation-n20.md` — session note on the original n=20 starvation finding, the work done so far to improve things, as well as some additional levers ("phase-2") we can consider implementing.

**The run (invocation gotchas that cost trial-and-error):**

- `./training/scripts/run_bc_modal.py --config training/data/sweeps/2026-06-05-dense-history-n/configs/n{5,10,20}.json --profile --skip-val --max-batches <~400> --detach` — reuse the *existing* sweep configs (same data/model the sweep already ran; H100 comes from `config.gpu`).
- The durable output is **`summary.json` on the `/runs` Volume**, not `run.log` (the report prints after the log tee closes). Pull with the `modal volume get` line the entrypoint prints.
- Local smoke that works: `./training/scripts/run_bc_local.py --config training/scripts/smoke-config.json --profile --skip-val --max-batches 5 --num-workers 2` — run from repo root; `--skip-val` is **required** (MPS val crash).

**Baseline to compare against (M1, n=5):** producer TOTAL ≈ **354 µs/sample**; `channel_build` 43%, `step_memory` 23%, `assemble` 21%. Byte-movement is ~free on M1 — so on Modal's bandwidth-bound CPU, watch for `assemble` and the broadcast channels in `channel_build` to balloon, and TOTAL to rise super-linearly n=5→20.

**Framing not in the code:** higher N performed *worse* (value-head overfitting), which was unexpected and needs future investigation. This tooling is **general infra to cut obs-build CPU cost**, useful across N. The n=20 cell is the current canary-in-the-coal mine, and has spurred us to really polish up the data pipeline and obs-building in a way that will support future iteration on our model design.

**Possible additional exploration:** we can consider trying a `py-spy --subprocesses` cross-check (needs `py-spy` in the image + a wrapper).

---

The first step is to re-do the 2 modal probes with n=5 and n=20 for dense-history with our new `--profile` tooling turned on. See the two configs we used: `training/tmp/n{5,20}_quick.json`. n=5 is the control. 

We can then pull down the run artifacts and panalyze the timing results.

Let me know when you are fully oriented and ready to go.
