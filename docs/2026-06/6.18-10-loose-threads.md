# Loose threads — argmin-toy / elim-head / fq line

*Date: 2026-06-18. Status: loose-thread catch (Phase 0c). Lightweight and short-lived — the live forward direction is [6.18-9](6.18-9-elim-head-synthesis-and-direction.md); this is the residue *not* on that critical path, captured so it isn't lost. Expect it to go stale within a week or two. No triage machinery on purpose.*

Anything genuinely on the path (the obs fix, the clean re-run, the enhancement landscape, the parked target decision, the Gap-1 representation work) lives in 6.18-9 and is not repeated here.

## fq + probe toolkit

These two are converging (6.17-3), so the infra items below span both. Almost all are gated on a real downstream consumer (the real aux-head / "P4") — don't build ahead of need:

- single-source the cross-player CE into a neutral home (`masked_cross_player_ce` is currently a hand-kept copy of prod's elim loss — latent divergence risk);
- where model-running derivers live (`FeatureSource`-as-deriver vs staying probe-side);
- migrating the probe walk onto the fq engine; synthetic/analytic row source; table caching; lazy-obs `FrameView` (+ `SimFrame` relocation off `dataset.py`);
- the `engine → fq` dependency-direction rule, the semantic-share boundary cases, `check_representative` auto-surfacing, the `emit_cols`-vs-`derivers` shape revisit, the dumb-container groupby relaxation — all "decide when a consumer forces it."
- `join_dump` standing test — natural fixture (yolo-11k ep5 dump) is gitignored, so it can't go in CI; a skipped stub with context sits in `tests/analysis/test_fq.py`.

Not infra (probe results / data):
- **Re-check 6.14-1's frozen-probe who-dies-next numbers on a representative eval split** — they used the small 110-game / 10k-frame default that 6.15-2's measurement lesson flagged; top-1 accuracy has no R²-collapse mode, but the small-game representativeness concern applies, and these are the load-bearing "no head beats the rule from any source" numbers. (Distinct from the obs/target confound in 6.18-9 §2.4.)
- The ELIM_ON vs ELIM_OFF army-regression comparison has a manifest/seed confound (6.14-1 Open 3); 6.15-2 judged it low-stakes given their near-equality, but it's unresolved.
- cross-checkpoint generalization of the head-trails-rule + mid-margin finding (only shown within one run across two epochs; other dumps exist, not run); ep3/ep4 epoch-curve fill-in (optional).

## argmin-toy Part 2 reserve

The 6.18-9 §3.D candidate set is the *re-prioritized* Part-2 menu. The original toy rider menu (6.17-1, 6.18-1 §12) is the broader reserve if the toy resumes: flatten family C, +land distractor, sample-complexity curves, regime-extrapolation, CE-vs-ranking-loss, order augmentation, width/activation/pooling-op sweeps, B1-depth-2, DeepSets-with-context (E2).

Cheap toy confirms left undone (low value with the toy paused): `ARMY_SIM → ARMY_OBS` swap; a clean A1/B1 row (raise `min_epochs`/`patience`); the `gap_scale` rescaled-notch control (likely moot — depth-1 cleared the notch).

## Code TODOs (this line)

- `bc/obs.py:348`, `:389`, `targets/elim_targets.py:77` — the obs/alive tick-seam banners → tracked as Chunk A in 6.18-9.
- `bc/obs.py:818` / `:821` — opponent rate channel currently emits zero (`opp_N_land_delta` heuristic; `5.05-1 §F` encoding). This is the code locus for the confirmed army-rate input gap (6.18-9 §2.5 / §3.C).
- `analysis/scripts/who_dies_next_baselines.py:41` — rewrite using fq (would simplify it; the re-run baselines in Chunk B are the natural moment).
- `analysis/obs_utils.py:40` — brittle hard-coded army-channel-name lookup; low-priority robustness.
- `analysis/argmin_toy/encode.py:116` — `broadcast` layout is `NotImplementedError` (toy Part-2 reserve).
- (Broader pre-existing `bc/` debt — `CITY_TRAVERSABILITY_FACTOR`, policy-channel magic numbers, `last_seen_*` sentinels, dataset return type — is out of this line's scope; grep `bc/` if doing a general cleanup.)

## Program-level / parked

- The who-dies-next aux-head *program* decision + the 24k-transfer bet (from 6.14-1) — still parked; none of this session's diagnostics moved it.
- Margin-distribution methodology — the error-vs-margin read is phase-dependent (opening near-ties vs late-game blowouts); no sampling policy was ever fixed.

## Doc hygiene (pending this session)

- Supersede pointers atop the §Next/§Open of 6.18-3/4/7/8 → 6.18-9 is the forward source.
- Record corrections: 6.13-15/6.14-1 (the ranking-step hypothesis is resolved — argmin is learnable, see 6.18-9 §2.1) and 6.18-7 §1.1–1.2 (baseline-as-representation-quality framing, and ranking-step-as-finding, both superseded).
- Stale lines: 6.18-8's "categorical vs boolean" open bullet (answered — no meaningful difference); 6.17-4 §4's "reuse `masked_cross_player_ce`" line (overturned by 6.18-1 — the toy is unmasked).

## Housekeeping

- Delete scratch spikes `fq/tier1_spike.py` and `tier1_spike_demo_clean.py` (repo root).
