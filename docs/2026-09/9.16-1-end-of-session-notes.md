This doc has some brief forward-looking notes extracted from the end of last session.

---

*Daniel:*

Let's talk about a possible reading list for next session orientation.

---

*Claude:*

The list depends on what next session takes on. Assuming it is the regen second pass, which pulls in the ledger and record design, I'd tier it like the 9.14-1 prompt did.

**Read first**

- 9.15-1. Still the primary handoff. Section 1 for the registry model, section 5 for the open items with today's update line.
- 8.12-1. The regen workflow sections, "Mid-development regen" and "Post-implementation workflow", and the "Regen tool notes" at the end. That is the authoritative text on baseline, "newly blessed versus changed", and the skeleton. Nothing newer supersedes it.
- 7.20-2. The boundary record field list and labels. The skeleton pre-fills from these.

**Read as needed**

- 9.13-1. The references layout, and the identical-groups rationale if the check needs revisiting.
- 8.21-1. The build item tiers, mainly for the open question on how the blocking test knows a record is open versus filled.
- 7.15-2. Scope authority, and section 4 for the environment record fields if that item comes up.
- `docs/confounds-framework.md`. Only for the impact labels when writing or pre-filling a record.
- The untracked conceptual sweep. Items 5 and 6 bear on the record design: supervision re-blesses are not reviewable in git, and the blocking-test trigger idea. Still unratified suggestions.

**Code**

- The git log for today's seven commits. The messages carry the what and why.
- `regen.py` in full, since it is the file being extended. About 230 lines.
- `registry.py`, for the entry and group types regen reads.

Two things I'd leave off: 9.12-1 and 9.04-1, which are superseded on everything that matters now, and the older design sketches below 7.20.

---

*Daniel:*

I think the regen second pass is a good direction? what are the other largest items you see?

There's the channel summary. And a lot of smaller details

---

**Claude**

Agreed on the regen second pass. The other large items, from the 9.15-1 build list plus the sweep:

- **Ledger and blocking test.** Really part of the regen cluster, since the record format is what the skeleton writes and what the blocking test reads. The format decision is the hub for all three.
- **Channel summary report.** The obs goldens' reviewable twin, since the hashes say a channel changed but not how. 7.22-2 has the sketch. The sweep's item 7, channel classes declared beside the channel definitions, is a prerequisite for the writer to not carry its own map. Independent of the regen cluster.
- **Dataset migration onto the walk core.** Medium size, but it gives the obs goldens a single producer and drops the parity test from the first cut. Independent of everything else. The sweep put it near the top and it came up again today under `compute_obs`.
- **Fixture selection.** 7.12-4 is the event-class checklist. Deliberately deferred to near the end, since it forces a real bless.
- **Layer-1 goldens.** Scope is the least clear of the big ones, and it carries the dependency-direction question that `SimMeta` was deferred to.

The smaller ones: environment record and its test, structural tests, the mutation test, the torch guards, and the workflow doc. The workflow doc I'd hold until the regen cluster settles, since that is most of what it describes.

Also queued before the first real bless, from 9.15-1: the `next_elim_dt` re-home, `value_target` as `[T]` versus scalar, and the checkpoint sweep for the time_bin edges.

If you want a rough shape:

- The regen cluster and the channel summary are the two design-heavy pieces left, and they don't depend on each other.
- The dataset migration is the largest pure-code item and could slot in whenever a lighter session is wanted.
- Fixture selection and the environment record come after the tooling is stable.

I would not commit to more ordering than that.

