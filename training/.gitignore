# Per-run training artifacts (logs + provenance).
# Manifests at data/splits/ stay tracked — leave-as-default unless we
# decide manifests are noise too.
data/runs/
data/runs-cloud/
data/probes/
data/bench/
data/sweeps/
